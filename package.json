{
  "name": "planova-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API and local server for the Planova UI",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "test": "node --test backend/tests/api.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
